import React from "react";

function Item({name,isLive}) {
  return{
    if(){
     
    }
  }
}

export default function CourseList() {
  return (
    <div>
      <ul>
        <Item name={`React Batch B1` } isLive={false}/>
        <Item name={`React Batch B2` } isLive={false}/>
        <Item name={`React Batch B3` } isLive={true}/>
      </ul>
    </div>
  );
}
